/** Number of data points kept for sparkline / area chart history. */
export const CHART_HISTORY_POINTS = 40

/** Time window used to identify "recent" network requests (ms). Mirrors monitor-api's window5s. */
export const RECENT_WINDOW_MS = 5_000

/** Max network entries shown in the inspector panel. */
export const INSPECTOR_MAX_REQUESTS = 5

/** Max events shown in the inspector panel. */
export const INSPECTOR_MAX_EVENTS = 6

/** Max entries shown in full-page log tables (Dashboard). */
export const LOG_MAX_ENTRIES = 20

/** Max slow React components shown in the inspector panel. */
export const INSPECTOR_MAX_SLOW_COMPONENTS = 5
